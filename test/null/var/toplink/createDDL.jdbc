CREATE TABLE OBJECTSHELFENTRY (ID INTEGER NOT NULL, DATA BLOB(64000), KEYNAME VARCHAR(255), shelfid VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE OBJECTSHELF (NAME VARCHAR(255) NOT NULL, PRIMARY KEY (NAME))
ALTER TABLE OBJECTSHELFENTRY ADD CONSTRAINT BJCTSHLFENTRYshlfd FOREIGN KEY (shelfid) REFERENCES OBJECTSHELF (NAME)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL, PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 1)
